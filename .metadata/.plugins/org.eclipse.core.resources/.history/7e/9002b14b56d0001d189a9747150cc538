package br.ba.fvc.controllrs;

import java.sql.ResultSet;
import java.util.ArrayList;

import br.ba.fvc.dao.GenericDAO;

public class GenericController {

	public GenericDAO dao;

	public GenericController() {
		this.dao = new GenericDAO();
	}

	public Object[] all(String table) {
		ResultSet result = null;
		ArrayList<Object> list = new ArrayList<>();

		try {
			result = this.dao.all(table);

			int coluna = result.getMetaData().getColumnCount();
			Object testando[] = new Object[0];

			while (result.next()) {

				ArrayList<Object> columns = new ArrayList<>(coluna);

				System.out.println(result.getRow());
				for (int i = 0; i < result.getRow(); i++) {
					System.out.println(result.getInt(i));
					// columns.add(i);
				}

				list.add(columns);
			}

		} catch (Exception e) {
			System.out.println(e.getMessage());
		}

		System.out.println(list);

		// System.out.println(data[0][1]);
		// System.out.println(data[0][2]);

		// System.out.println(data[0][1]);
		return list.toArray();
	}
}
